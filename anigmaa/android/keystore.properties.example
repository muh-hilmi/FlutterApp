# Production Keystore Configuration
# Copy this file to keystore.properties and update with your actual values
# Generate keystore with: keytool -genkey -v -keystore anigmaa-release.keystore -alias anigmaa -keyalg RSA -keysize 2048 -validity 10000

storeFile=../keystore/anigmaa-release.keystore
storePassword=YOUR_KEYSTORE_PASSWORD_HERE
keyAlias=anigmaa
keyPassword=YOUR_KEY_PASSWORD_HERE
